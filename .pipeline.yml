steps:
  - name: Install Dependencies
    run: pip install -r requirements.txt

  - name: Run Tests
    run: pytest

  - name: Build Docker Image
    run: docker build -t app-image .

  - name: Deploy Container
    run: docker run -d --name app-container app-image
